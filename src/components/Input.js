import { TextInput } from "react-native"

const Input = () => {
  return (
    <TextInput
      style={{
        borderWidth: 1,
        borderColor: 'yellow',
        padding: 4
      }}
      placeholder="Your input here..."
    />
  )
}
export default Input